# TODO — Financial Tools visibility rules

- [ ] Add CSS class to hide panels (is-hidden) if not present.
- [ ] Update `products.html` hero quick links to be usable (ids/selection hooks if needed).
- [ ] Update `products.js`:
  - [ ] Implement `applyToolVisibility(tool)` mapping Fund. / Loans&Debt / Investment to which panels are shown.
  - [ ] Wire hero quick link clicks to activate corresponding tab + apply visibility.
  - [ ] Ensure deep-link via URL hash also applies visibility on load.
- [ ] Test manually in browser: click hero links and tabs; confirm SIP/Loan/Ratios show/hide per requirements.

