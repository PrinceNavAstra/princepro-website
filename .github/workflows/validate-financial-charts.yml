name: Validate financial chart code
on:
  push:
    paths:
      - '.github/workflows/validate-financial-charts.yml'
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check financial-tools-fix.js
      - run: git diff --check HEAD~1 HEAD || true
